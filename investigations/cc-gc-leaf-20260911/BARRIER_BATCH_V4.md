# Fresh-origin observation with real emitted-IR controls

Use `fresh_barrier_batch_v4.py` with the final `audit-v2/helper-audit.csv`: 80 existing CannotCollect summaries and 48 independently reviewed additional hypotheses. This is an observer, not a compiler optimization. Earlier scripts and receipts remain unchanged.

The primary arm intersects identical allocation-origin facts across every reachable predecessor. Entry and initial predecessor facts are empty. A possibly collecting call clears both SSA and eligible scalar-slot facts. The output also retains the earlier no-alias and blanket-join-kill counts, so additional analytical coverage is visible rather than mistaken for a new program property.

Local aliases are restricted to exact scalar alloca stores/loads whose complete address-use ledger contains no escape, alias, GEP, volatile, atomic, or lifetime uses; exact empty tied-register assembly; and 64-bit identity casts. The batch requires the actual little-endian x86_64 Linux DataLayout and integral 64-bit AS0/AS1. AS270/271 and every unproved address space decline. Phi, select, tag arithmetic, and other loads remain unresolved. Unparsed or unsupported functions are explicitly excluded; their counts are unavailable, not zero.

Twelve local controls pass. They include equal-origin diamonds, one collecting arm, different allocation origins, loops, address-space truncation, slot escape, and planted false-summary/assembly failures. `barrier_actual_fixture_check.py` additionally analyzes the exact successfully emitted fixture `pre-rs4gc.ll`, SHA `1fb2ecc2ba6d144083603618176d9e065deef945d7bdd7030dbd54bcc00ecb3e`. It admits all 18 functions and finds five explicit reachable heap-barrier sites. Two constructor sites have a proved allocation origin without an intervening potentially collecting call (original IR lines 6490 and 7176); three parents remain unresolved. The 48 extra summaries add zero sites. Blanket join killing found zero of five.

The real-input counterfactual adds one unknown collecting call to an existing root-barrier arm. Exactly the specialized constructor finding disappears: two fresh-origin sites become one, with the same five-site denominator and 18 admitted functions. This is text analysis of real emitted code plus a deliberate counterfactual, not native execution. Its files and exact identities are in `barrier-actual-fixture-v1/v4-real-input-receipt.json`.

No full-cc barrier result is claimed yet. The later locked run must bind completed accepted compilation attempts, the lossless text conversion receipt, final helper audit, and analyzer versions. Root must separately bind accepted compiled units to the executable. Explicit sites are not execution counts; hidden runtime stores, already omitted barriers, and root/layout operations are outside the denominator. Fresh allocator returns can already be old or published. Existing `ctor_prologue_stores.rs` explicitly retains generation-tested and incremental work on fresh fields. Neither generational nor incremental/layout/string-sharing removal is proved by these counts.
